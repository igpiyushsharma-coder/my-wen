# Copy this file to .env and fill in real values if you want to try
# the optional live AI API integration in services/ai_service.py.
# The app works completely fine without this file -- it runs on
# sample/uploaded data (Demo Mode) by default.

AI_API_KEY=your_api_key_here
